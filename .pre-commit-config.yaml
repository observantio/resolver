repos:
  - repo: local
    hooks:
      - id: becertain-unit-tests
        name: BeCertain unit tests
        language: system
        entry: bash -lc 'python -m pytest -q'
        pass_filenames: false
        always_run: true
